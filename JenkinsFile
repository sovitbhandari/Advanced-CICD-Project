pipeline {
  agent {
    docker {
      image 'python:3.12-slim'  // Official Python Docker image
      args '--user root -v /var/run/docker.sock:/var/run/docker.sock'
    }
  }

  environment {
    DOCKER_IMAGE = "yourdockerhubusername/python-flask-app:${BUILD_NUMBER}"
    REGISTRY_CREDENTIALS = credentials('docker-cred')
  }

  stages {
    stage('Checkout') {
      steps {
        sh 'echo Cloning Repository...'
        sh 'ls -la'
        // Optional: Uncomment if you want to pull from Git
        // git branch: 'main', url: 'https://github.com/your-username/your-python-repo.git'
      }
    }

    stage('Install Dependencies') {
      steps {
        sh '''
          pip install --upgrade pip
          pip install -r requirements.txt
        '''
      }
    }

    stage('Lint and Test') {
      steps {
        sh '''
          pip install flake8 pytest
          flake8 .
          pytest || echo "No tests defined"
        '''
      }
    }

    stage('Build and Push Docker Image') {
      steps {
        script {
          sh 'docker build -t ${DOCKER_IMAGE} .'
          def dockerImage = docker.image("${DOCKER_IMAGE}")
          docker.withRegistry('https://index.docker.io/v1/', "docker-cred") {
            dockerImage.push()
          }
        }
      }
    }

    stage('Deploy or Update Deployment File') {
      steps {
        sh 'echo Deployment Stage - Add your deployment steps here'
      }
    }
  }
}
